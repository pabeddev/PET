{
  "scripts": {
    "build": "CI=false npm run build_front && npm run move_dist && npm run build_server",
    "build_front": "cd client && npm run build",
    "move_dist": "cp -r client/build/* server/public/",
    "build_server": "cd server && npm run build",
    "start": "cd server && node main.js",
    "vercel-build": "CI=false npm run build"
  }
}
