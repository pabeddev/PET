{
  "scripts": {
      "build": "npm run build_front && npm run build_server",
      "build_front": "cd client && npm install && npm run build && cd .. && npm run move_dist",
      "move_dist": "cp -r client/build/* server/public",
      "build_server": "cd server && npm install",
      "start": "cd server && npm start",
      "vercel-build": "npm run build_front && npm run build_server && npm run start"
  }
}
